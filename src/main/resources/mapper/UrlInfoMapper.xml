<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cy.shorturl.mapper.UrlInfoMapper">
    <insert id="insertUrlInfo" parameterType="com.cy.shorturl.entity.UrlInfo">
        insert into url_info (url,create_time)
        values (#{url},#{createTime})
    </insert>

</mapper>
